# Nomad Single Node Images

Nomad single-node images for use with real servers. These images will run the Nomad agent as both a server and a client simultaneously.

## Usage

```sh
packer build nomad-ubuntu.pkr.hcl
```
